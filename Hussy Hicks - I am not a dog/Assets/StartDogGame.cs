
using UnityEngine;
using UnityEngine.SceneManagement;

public class StartDogGame : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
